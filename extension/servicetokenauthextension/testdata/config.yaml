service_token_auth:

service_token_auth/empty_endpoint:
  endpoint:

service_token_auth/empty_url:
  endpoint:
    url:

service_token_auth/missing_cache:
  endpoint:
    url: http://localhost:8091/authorize

service_token_auth/empty_cache:
  endpoint:
    url: http://localhost:8091/authorize
  cache:

service_token_auth/valid:
  endpoint:
    url: http://localhost:8091/authorize
  cache:
    valid_size: 10
    valid_ttl: 20s
    invalid_size: 30
  schema:
    StackStack2
